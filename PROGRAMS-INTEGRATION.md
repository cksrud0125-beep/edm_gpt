# edm 공식 프로그램 추천 연결

- `src/data/programs.json`: 공식 목록 13페이지에서 수집한 중복 없는 제목, 소개, 이미지 URL, 상세 URL 및 확인 시각.
- `npm run sync:programs`: 공개 목록을 다시 가져옵니다. 네트워크 접근이 필요합니다. 실패하면 기존 카탈로그를 보존합니다.
- `npm run test:recommendations`: 대상 연령, 국가, 분야 분리 및 링크 검증.
- 갱신 후 `npm run build`로 재빌드합니다. 개발 미리보기에는 HMR로 반영됩니다.

현재는 저장된 공식 목록에 규칙을 적용하는 추천입니다. 관리자 API, LLM API, 실시간 가격 및 모집 상태 연동은 없습니다. 제목과 URL로 분류하며, 분류되지 않는 항목이나 모집 상태를 검증할 수 없는 시한성 행사 등은 추천에서 제외합니다. 목록에 없는 링크를 만들지 않습니다. 상세 자격/예산/일정은 적합 판정하지 않고 상담 필요로 표시합니다.

공식 상담 신청은 외부 링크로 이동합니다. 조건 자동 전달 API는 확인되지 않아 사용자가 조건을 복사해 문의사항에 붙여 넣도록 했습니다. 자동 갱신이 필요한 운영 환경에서는 서버의 정기 작업으로 동기화 명령과 배포를 연결하거나 edm 관리자 API로 교체해야 합니다.
